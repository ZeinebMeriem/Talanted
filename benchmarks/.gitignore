# Benchmarking results (large, temporary files)
*.csv
*.json
!collect_baseline.py  # Keep baseline collection script
!locust_tasks.py      # Keep load test script
!README.md           # Keep documentation

# Test artifacts
*.log
*.html
__pycache__/
*.pyc
.pytest_cache/
.coverage

# Keep the results directory but not its contents
!.gitkeep
